# UML 图快速查看指南

## 🚀 最简单的方法（推荐，无需安装）

### 使用在线工具查看

1. **打开任意 `.puml` 文件**（例如：`02_complete_dialogue_sequence.puml`）
2. **全选复制**（`Cmd + A` 然后 `Cmd + C`）
3. **访问在线工具**：http://www.plantuml.com/plantuml/uml/
4. **粘贴内容**，自动生成图片

✅ **优点**：无需安装任何软件，即开即用

---

## 💻 如果想在 VS Code 中预览（需要安装 Java）

### 快速安装 Java

#### 方法1：使用 Homebrew（推荐）

```bash
# 安装 OpenJDK（免费开源）
brew install openjdk

# 或者安装 Adoptium（推荐，免费且稳定）
brew install --cask temurin
```

安装完成后：
1. **重启 VS Code**
2. 打开 `.puml` 文件
3. 按 `Alt + D` 预览

#### 方法2：下载安装包

1. 访问：https://adoptium.net/
2. 选择 macOS 版本下载
3. 安装后重启 VS Code

---

## 📋 验证 Java 安装

安装后运行：

```bash
java -version
```

应该显示类似：
```
openjdk version "17.0.x" ...
```

---

## 🎯 推荐方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **在线工具** | 无需安装、即开即用 | 需要网络 | ⭐⭐⭐⭐⭐ |
| **安装 Java + VS Code** | 本地预览、方便编辑 | 需要安装 Java | ⭐⭐⭐⭐ |
| **本地 PlantUML** | 可生成图片文件 | 需要安装 Java 和 PlantUML | ⭐⭐⭐ |

---

## 📁 所有 UML 文件

所有文件位于：`docs/uml/`

- `01_core_class_diagram.puml` - 核心类图
- `02_complete_dialogue_sequence.puml` - 完整对话流程 ⭐
- `03_intent_comparison.puml` - 意图识别对比 ⭐
- `04_tool_call_sequence.puml` - 工具调用流程
- `05_custom_plugin_loading.puml` - 自定义插件加载
- `06_intent_decision_activity.puml` - 意图识别决策
- `07_tool_call_decision.puml` - 工具调用决策
- `08_audio_processing.puml` - 音频处理流程
- `09_component_diagram.puml` - 系统组件关系
- `10_connection_state.puml` - 连接状态转换
- `11_mcp_call_sequence.puml` - MCP调用流程
- `12_custom_plugin_dev.puml` - 自定义插件开发

⭐ 标记为重要图表，建议优先查看

---

## 🔧 如果遇到问题

### VS Code 插件找不到 Java

1. 打开 VS Code 设置（`Cmd + ,`）
2. 搜索 `plantuml`
3. 找到 `PlantUML: Java` 设置
4. 填入 Java 路径：

查找 Java 路径：
```bash
which java
# 或
/usr/libexec/java_home
```

### 其他问题

查看详细指南：`VIEW_GUIDE.md`

